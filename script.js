const darkModeToggleButton = document.getElementById('darkModeButton')
darkModeToggleButton.addEventListener('click', () => {
    document.body.classList.toggle('darkMode')
    // if darkModeToggleButton 
});